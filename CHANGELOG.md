# 更新日志

## 未发布

### 修复
- **菜单删除现在会级联清理**：删除菜单时同时删除该菜单下的所有反馈和回复，不再留下孤儿数据。
- **未知 `/api/*` 路径返回 JSON 404**：过去会返回 `index.html`，客户端误以为是页面路由。
- **菜品描述加长度校验**：单条描述 ≤ 200 字符，超出返回 `400 bad_dish_description`。
- **打星和公开/私密切换不再丢失输入焦点**：这两个操作现在只更新对应 CSS 状态，不重建整个抽屉，textarea 的光标位置、输入法状态、已输入文字全部保留。
- **「退出管理」按钮**：改为单击直接退出，之前用的原生 `confirm()` 弹窗在某些嵌入式浏览器里会被静默拦截。

### 变更
- 品牌重命名为 **W-HOUSE**（原膳意），logo 由 `膳` 改为 `W`。
- 早餐板块改为横向压缩带：左侧「早餐 · 全周共用」标题（16px，与 day 名字号一致），右侧三列 主食/小菜/饮品，每列最多 4 行。
- 「午餐 / 晚餐」小标题在 day 卡片内加大到 14px 显示字体，圆点改用主色调。
- 反馈计数拆分：`avg rating`（只统计打分 > 0）和 `feedbackCount`（所有反馈，含纯文字评论）独立展示，纯文字提交也会更新反馈条数。
- 菜品总数按名字去重计算，允许同名菜品出现在不同日期/时段。

### 已验证
- 39 项自动化测试覆盖：住客提交/查看/删除、管理员登录/发菜单/回复/删除、私密隔离、XSS 存储与渲染、字段边界（长度、数值 clamp）、菜单删除级联。

## 说明

版本号还没定式发布，先以本文件为准。生产部署遵循 [DEPLOY-ALIYUN.md](DEPLOY-ALIYUN.md)。
