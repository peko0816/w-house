# W-HOUSE 管理员操作手册

面向物业餐厅管理员的日常使用指南。全程 **10 分钟**读完即可上手。

访问入口：`http://8.129.229.177:8080`（或你的正式域名）

---

## 目录

- [1. 初次登录](#1-初次登录)
- [2. 发布本周菜单](#2-发布本周菜单)
- [3. 编辑 / 删除已发布的菜单](#3-编辑--删除已发布的菜单)
- [4. 查看住客反馈](#4-查看住客反馈)
- [5. 回复反馈](#5-回复反馈)
- [6. AI 起草回复（可选）](#6-ai-起草回复可选)
- [7. 常用操作与技巧](#7-常用操作与技巧)
- [8. 常见问题 FAQ](#8-常见问题-faq)
- [9. 安全与备份](#9-安全与备份)

---

## 1. 初次登录

1. 手机 / 电脑浏览器打开系统地址
2. 首次访问会弹出「请输入昵称」窗口——**你也需要填一个**（例如"管理员"或"物业前台"），系统靠昵称显示你的身份
3. 点击右上角的 **管理员** 按钮 → 弹出密码框 → 输入管理员密码 → 登录成功
4. 登录后顶部会多出一排**工具栏**：

   ```
   ＋ 新一周菜单    ✎ 编辑本周    ◈ 反馈管理    ✕ 删除本周              退出管理
   ```

登录信息保存 **30 天**，30 天内同一设备无需重复登录。

---

## 2. 发布本周菜单

### 2.1 点击 **＋ 新一周菜单**

打开菜单编辑器抽屉。默认起始日是"下周一"，也可以点日期选择器改到任意周一。

### 2.2 填写菜品

编辑器分三大板块：

#### 早餐（全周共用）

按 **主食 / 小菜 / 饮品** 三栏各自填写，一行一道菜。整周的早餐通用一份，不用每天填一遍。

推荐每栏不超过 4 行，太多也放不下。

#### 午餐 / 晚餐（每日 4 菜 1 汤）

7 天 × 2 餐 = 14 个格子，每格 5 行（4 菜 + 1 汤）。

### 2.3 「|」加菜品描述（可选）

想在菜名旁标注辣度、食材等信息时，用竖线 `|` 分隔：

```
宫保鸡丁 | 微辣，含花生
麻婆豆腐 | 微辣
清炒时蔬
番茄鸡蛋汤
```

- **左边**是菜名（住客点开详情看到大字）
- **右边**是描述（列表中鼠标悬停时浮出，详情页里显示在菜名下方）

允许**不同日期出现同一道菜**（比如"青椒肉丝"周二午餐和周日晚餐都上），去重后计入"本周菜品总数"。

### 2.4 点 **发布** → 完成

菜单立即对所有住客可见。发布后头部会显示 "**发布于刚刚 · N 道菜 · 0 条反馈**"。

---

## 3. 编辑 / 删除已发布的菜单

### 3.1 编辑

- 顶栏点 **✎ 编辑本周** → 打开当前正在展示的那一周的菜单编辑器
- 修改后点 **保存**（不会重置发布时间）
- **技巧**：**同名菜品**在编辑前后保留同一个内部 ID，历史评分不丢失；改名字（哪怕只改一个字）会被视为新菜品，历史评分从零开始

### 3.2 删除本周菜单

- 顶栏点 **✕ 删除本周** → 弹确认对话框 → 确认
- **⚠️ 删除会级联清理该菜单下的所有反馈和回复**（不可恢复）
- 场景：删除测试数据、误发布、或者一周结束想清空重来

### 3.3 切换查看历史 / 未来菜单

顶部中间 `‹ 第 XX 周 ›` 左右箭头可以翻看：

- **←**：上一周（一直翻到最早）
- **→**：下一周（到最新一周）

编辑器打开的**永远是当前正在展示**的那周菜单，先翻到目标周再点 **✎ 编辑本周**。

---

## 4. 查看住客反馈

顶栏点 **◈ 反馈管理** → 打开反馈管理抽屉。

### 4.1 三个 tab

| Tab | 说明 |
|---|---|
| **全部** | 本周所有反馈，按时间倒序 |
| **待回复** | 还没写回复的反馈（这个数字就是"未处理"） |
| **私密** | 住客选择"仅管理员"标记的反馈 |

每个 tab 名字旁的圆圈数字实时更新。

### 4.2 一条反馈显示什么

```
菜品名  [私密]  ★★★★☆  住客昵称  2 小时前
反馈正文（如果只打了分没写字，显示"（无文字）"）
物业回复气泡（如果已回复）
[回复] [自动生成回复] [删除]
```

- **私密**标签只在私密反馈上出现
- **删除**是永久删除（连带删掉这条反馈的回复），谨慎使用

### 4.3 关注哪些反馈

- 优先看**待回复** tab 里 **1-2 星**的批评，这是要优先响应的
- **私密** tab 里的反馈**只有管理员能看到**——住客不希望公开的抱怨或建议都在这里
- 高星表扬也建议顺手回一句，住客会感到被听见

---

## 5. 回复反馈

### 5.1 点 **回复** 按钮

反馈下方出现一个内联表单：

```
┌────────────────────────────┐
│ 回复给住客的话…              │
│                            │
└────────────────────────────┘
              [取消] [发送]
```

写好内容点 **发送**，会立即出现在这条反馈下方的橙色气泡里。**住客本人**下次打开系统就能在 **"我的反馈"** 面板里看到。

### 5.2 编辑已发的回复

- 已回复的反馈按钮从 **回复** 变成 **编辑回复**
- 点开修改保存 → 覆盖原回复

### 5.3 回复措辞建议

- 30-100 字为宜
- 具体回应住客说的内容：表扬 → 感谢并转达厨师；抱怨 → 先致歉再说明改进方向；建议 → 感谢并说会与厨房沟通
- 别写"感谢您的反馈，我们会持续改进"这种模板话，读起来敷衍

---

## 6. AI 起草回复（可选）

如果服务器配置了 `ANTHROPIC_API_KEY`，每条反馈下会多一个 **自动生成回复** 按钮，或者在回复表单里有一个 **自动生成** 按钮。

**用法**：
1. 点开一条反馈的**回复**表单
2. 点里面的 **自动生成** 按钮 → AI 会根据菜品名、住客评分和文字反馈起草一段礼貌温暖的回复文本，自动填到输入框里
3. **人工检查 / 修改**后点 **发送**
4. 发出的回复会标记 **AI 起草** 小标签（住客看得到，透明处理）

**注意**：
- AI 起草只是**辅助**，最终发出的内容你负责
- AI 生成失败时（网络问题、API 额度不够）会弹提示，可以直接手写

如果你的部署还没配 API Key，`自动生成` 按钮不会出现，纯手动回复即可。

---

## 7. 常用操作与技巧

### 7.1 一次批量处理反馈

星期一早上开始上班，打开 **反馈管理** → 切到 **待回复** tab → 逐条回复 → 关闭抽屉。一般 5-10 分钟处理完一周所有未响应反馈。

### 7.2 复制上周菜单快速新增

目前没有直接的"复制"按钮，但可以这么做：

1. 翻到上周 → **✎ 编辑本周** → 抽屉里全选所有菜品文字复制
2. **取消** 关闭
3. 翻到下周 → **＋ 新一周菜单** → 把文字粘贴到对应格子里 → 微调
4. **发布**

### 7.3 让"私密反馈"的住客也能看到你的回复

私密反馈虽然其他住客看不到，但**回复本身在本机的"我的反馈"里能看到**——只要住客还在同一设备上，就能追到你的回复。跨设备的话可能就看不到（那是住客自己账号系统缺失的问题）。

### 7.4 快速统计本周口碑

反馈管理抽屉里，**全部** tab 的数字就是本周总反馈条数。想看具体菜品的分：回到主页在网格里每个菜名右边 `★ 4.5 · 3 条` 就是当前分数 + 反馈数。

---

## 8. 常见问题 FAQ

### Q: 忘记管理员密码怎么办？

只能通过服务器改。SSH 登录到服务器：

```bash
nano /opt/w-house/.env
# 找到 ADMIN_PASSWORD=xxx 那行改成新密码
pm2 restart w-house
```

改完立即生效，用新密码登录即可。

### Q: 住客提交的评价能删掉吗？

**只有管理员能删**——反馈管理里每条右下角有个红色的 **删除** 按钮。删除后**不可恢复**，连带回复一起删。

### Q: 我不小心发布错了菜单可以撤回吗？

- 想全删重来：点 **✕ 删除本周**
- 想改内容：点 **✎ 编辑本周** 修改保存

住客那边不会有专门的"新版本"通知，他们只在下次刷新页面时看到最新内容。

### Q: 星标分数怎么算的？

- **只有打了星的反馈**才计入平均分（打星 = 1-5 星，"未评分"的不算）
- **反馈条数**统计**所有**提交的反馈（含只文字没打星的）
- 所以你会看到有些菜"没有平均分但有 X 条反馈"的情况——住客写了评论但没打分

### Q: 一个住客能提交多次反馈吗？

能。每次提交都会新增一条独立记录。同一住客也可以给同一道菜打两条不同评价（比如上午吃了觉得不错，晚上又来评一次）。

### Q: 昵称能重复吗？

能。系统靠**浏览器本机的匿名 ID**区分不同住客，不靠昵称唯一性。所以你会看到多个"张阿姨"的反馈——他们可能是同一人也可能不是，从技术上区分不了。

### Q: 私密反馈住客本人能看到吗？

住客在**同一设备**上通过右下角"我的反馈"能看到自己所有的提交（含私密）。换设备（比如从手机换电脑）就看不到了——这是系统当前限制。

### Q: 想改「W-HOUSE」这个品牌名字怎么办？

需要改代码。改动 `public/index.html` 里的 `<title>` 和 `brand-title`，改动 `public/app.js` 里所有 `W-HOUSE` 字符串。改完 git push，服务器 `git pull` 即可。

---

## 9. 安全与备份

### 9.1 定期改密码

至少 3 个月改一次管理员密码。做法同上（改 `.env` 里 `ADMIN_PASSWORD` → `pm2 restart w-house`）。

### 9.2 SQLite 数据备份

反馈内容和菜单数据都在 `/opt/w-house/data.sqlite` 一个文件里。**手动**备份：

```bash
cp /opt/w-house/data.sqlite ~/w-house-backup-$(date +%F).sqlite
```

**自动**每天备份：SSH 到服务器 → `crontab -e` → 加一行：

```
0 3 * * * cp /opt/w-house/data.sqlite /root/backups/data-$(date +\%F).sqlite && find /root/backups -name 'data-*.sqlite' -mtime +30 -delete
```

每天凌晨 3 点自动备份，保留最近 30 天。

### 9.3 恢复备份

停服务 → 覆盖 → 起服务：

```bash
pm2 stop w-house
cp ~/w-house-backup-2026-09-13.sqlite /opt/w-house/data.sqlite
pm2 start w-house
```

### 9.4 想让所有住客重新填昵称

暂时没有"退出所有住客"按钮。**变相方案**：修改 `.env` 里的 `SESSION_SECRET` 换个新值 → `pm2 restart w-house`，会让所有已登录的**管理员**被退出。**住客那边**的昵称存在浏览器 localStorage 里，服务端没有清理入口。

---

## 联系与升级

代码仓库：https://github.com/peko0816/w-house

想加新功能（如：住客微信登录、菜品图片上传、每周口碑汇总邮件、Excel 导出反馈等），提 issue 或直接找部署方沟通即可。

**日常运维在 [DEPLOY-ALIYUN.md](DEPLOY-ALIYUN.md) 里有详细命令**：更新代码、重启、看日志、故障排查等。
